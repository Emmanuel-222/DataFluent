const GradientBackground = () => {
  return (
      <div
        style={{
          background:
            "radial-gradient(circle at top left, rgba(0, 255, 200, 0.1), rgba(0, 0, 0, 0.1))",
        }}
      >
      </div>
  );
};

export default GradientBackground;
